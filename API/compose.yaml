version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: API/Dockerfile
    container_name: h2-api-johotel
    ports:
      - "9022:8080"   
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      DATABASE_URL: "Host=ep-damp-grass-a2y11rk5-pooler.eu-central-1.aws.neon.tech;Username=neondb_owner;Password=npg_idOpHYQ8JD5E;Database=neondb;sslmode=require"
      Jwt__Issuer: "MyHotelApi"
      Jwt__Audience: "MyHotelFrontend"
      Jwt__SecretKey: "SuperSecretKey12345678910111213141516asdfghjkl!?JakobJensen"
      # helps when behind a reverse proxy/https:
      ASPNETCORE_FORWARDEDHEADERS_ENABLED: "true"
    restart: unless-stopped
    networks:
      - h2-network

  blazor:
    build:
      context: .
      dockerfile: Blazor/Dockerfile
    container_name: h2-blazor-johotel
    ports:
      - "9021:80"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - h2-network

networks:
  h2-network:
    driver: bridge
